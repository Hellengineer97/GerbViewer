<svg xmlns="http://www.w3.org/2000/svg"
     viewBox="{{ viewbox | map('round', 1) | join(' ') | default('0 0 400 200') }}"
     preserveAspectRatio="xMinYMin meet">
     {% block content %}{% endblock %}
</svg>
